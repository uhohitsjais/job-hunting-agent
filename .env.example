# OpenAI Responses API — required starting Milestone 4 (scoring), not needed for M1-M3
OPENAI_API_KEY=
# Set to whichever current OpenAI model you want to use for scoring/writing
OPENAI_MODEL=

# Optional overrides — defaults work fine for local use
DATABASE_PATH=data/jobs.db
FLASK_PORT=5000
